[tool.poetry]
name = "trimero_mod"
version = "0.1.0"
description = "Simulación de trimero atómico en Python"
authors = ["Javier Aguilera <aguilerajavier58@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11"
numpy = "^2.3.1"
scipy = "^1.16.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
